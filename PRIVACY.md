# 数据使用说明

## 首次启动

默认处于暂停状态。屏幕识别、网课声音转写、网课 OCR、摄像头、麦克风持续录音和在线语音均关闭。个人配置文件不能自动开启这些开关。启动时不会读取 Obsidian 库、聊天归档、浏览器登录信息、密码或 API 密钥。

## 使用者开启后会发生什么

| 功能 | 输入和处理位置 | 主动保存的内容 | 对外发送 |
| --- | --- | --- | --- |
| 屏幕识别 | 主显示器图像 → 本机视觉模型 | 时间、活动类别、活动描述、课程提示；不主动保存原始截图 | 发送到本机回环地址 |
| 桌面互动 | 活动信息 → 同一本机模型 | 提醒文案、规则和投递时间 | 发送到本机回环地址 |
| 网课声音转写 | 电脑播放的声音 → 临时 WAV → 本机 Whisper | 转写文字；完整 WAV 转写尝试后删除 | 不发送给云端转写服务 |
| 麦克风 | 使用者主动开启后采集周围声音 → 本机 Whisper | 临时 WAV、转写文字；停止后释放设备，未完成片段丢弃 | 不发送给云端转写服务 |
| 网课 OCR | 被识别为网课时的整屏图像 → 本机 OCR | 提取文字，可能包含窗口周边内容 | 不发送给云端 OCR 服务 |
| 摄像头 | 按间隔抓一帧 → 本机视觉模型 | 行为判断及说明；不主动写出原始图片 | 发送到本机回环地址 |
| 日报 | 仅本项目的数据目录 → 本机模型 | 日报素材、Markdown 日报；已有同名日报先做本地备份 | 发送到本机回环地址 |
| 在线语音（可选） | 提醒文案 → edge-tts 在线服务 | 临时 MP3，播放尝试结束后删除 | **文案会离开本机，可能包含屏幕推导内容** |

“不主动保存原图”描述的是本程序的写文件行为，不保证操作系统、第三方运行时、内存换页或其他软件不会保留数据。提示模型少输出个人信息不能保证它不会误输出；运行日志仍按私人数据处理。

## 本机和联网边界

- 屏幕、摄像头、互动与日报只使用字面回环 IP 的 HTTP 模型接口。程序忽略环境代理和 netrc，不跟随模型服务重定向。
- 使用 Ollama 前检查模型的 remote_host、remote_model 和本地模型元数据；验证不了则不发送学习内容。不自动回退到外部服务。
- 此保护依赖受信任的本机服务。自建代理、被修改的模型服务或其他进程不在代码保证范围内；按官方说明关闭 Ollama 云功能。
- 依赖与模型由使用者主动联网安装。Whisper 运行时启用离线加载。没有后台学习记录上传、云端日报代理或使用统计上报。
- 在线语音是明确标注的例外，默认关闭；菜单显示解释并询问后才开启。

## 暂停与退出

“暂停全部采集”停止后续屏幕采集、系统声音和麦克风录制，并关闭摄像头、OCR、在线语音开关。已经进行中的设备调用和本机推理可能有短暂结束延迟。已经写出的转写或日报不会因暂停而消失，已经发出的在线请求不能撤回。

恢复屏幕识别后，声音、OCR、摄像头和联网语音仍需分别开启。退出后，下次启动又回到全关闭状态。

## 存储与清理

- data/logs：事件、转写、OCR、界面通信、运行错误及日报素材，可能含私人信息。
- 带日期的事件、转写、OCR、仪表盘：启动清理时处理超过 14 天的文件；不是到点立即删除。
- 滚动运行日志超过 20 MB 时截断保留尾部；临时孤儿 WAV 超过一小时后在启动清理中处理。
- 日报、日报备份与 report-material 素材不在上述日期清理列表内，需要使用者自己管理。项目没有自动加密这些文件。
- 默认日报在 data/reports；也可指定 Obsidian 目录，仅写选定的报告，不扫描知识库。

## 分享代码或反馈问题

只分享审核过的源码包。不要附带 data、logs、config.local.json、环境变量文件、录音、截图、模型缓存、个人备份或凭据。反馈问题优先提供复现步骤和虚构示例；确需日志时逐行检查并脱敏。
