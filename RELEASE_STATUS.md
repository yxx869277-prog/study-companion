# v0.1.0-alpha.1 验证记录

本项目为 Windows 单机实验版。下列检查于 2026-09-07 至 2026-09-08 完成，使用虚构内容，不构成准确率、最低硬件配置或长期稳定性的保证。

## 已完成

| 检查 | 结果与范围 |
| --- | --- |
| 独立环境安装 | Windows 11 x64 / Python 3.11.15 新建虚拟环境，基础与可选依赖均安装成功；pip check 通过 |
| 16 项回归测试 | 默认不采集、主动启用、录音取消与释放、暂停关闭开关、本机网络边界、CPU 请求参数、日报备份等测试通过 |
| 真实桌面 UI | Tk 演示窗口创建与结束正常；完整主程序在采集/模型调用被阻断的验证环境中启动为暂停状态，正常退出且没有触发禁止调用 |
| 真实 OCR | RapidOCR 从程序生成的英文数学课件中提取文字成功；单次示例约 9 秒 |
| 真实视觉与文本模型 | Ollama 0.33.3 + qwen3-vl:4b-instruct，采用最终默认 CPU 参数，虚构课件被识别为网课，导数回答正确；两次请求合计约 55 秒，包含加载时间 |
| 真实本机日报 | 使用虚构课件文字生成 Markdown 日报，约 27 秒，没有走降级摘录 |
| 真实语音转写模型 | 本地 faster-whisper-small / CPU int8，读取由系统离线合成的虚构中文语音，正确转写三句话并写入测试目录；约 6 秒 |
| 来源与分发范围 | 核对项目源码、搭建记录、直接依赖许可及安装环境依赖清单；只分发源码与文档，不捆绑环境、权重、二进制或系统素材 |

演示入口 demo.py 不启动真实采集或模型服务。测试时间只是这台电脑和这些输入的观察值，不能作为每台电脑的速度承诺。

## 当前限制

- 本轮未采集真实屏幕、麦克风、系统声音或摄像头；设备驱动、实际录音质量和长期运行仍需使用者在知情开启后验证。模拟设备测试不能代替这些验证。
- 未完成独立实体电脑及不同 Windows/显卡驱动的兼容性测试。GitHub 自动检查只验证安装和不接触设备的回归测试。
- Ollama 自动加速路径曾返回内存分配失败；最终默认采用已验证的 CPU 参数。启用显卡加速需另行验证。
- llama.cpp 是可选实验后端，本轮未执行该后端的端到端验证。
- 图像分类、OCR、语音和模型日报都可能出错；单个示例成功不代表总体准确率。长日报可能超出配置的上下文容量。
- 在线语音只验证了依赖安装，未调用外部服务。它默认关闭，开启后会发送播报文案。
- 当前提供源码和 Python 安装步骤，没有 Windows 独立安装器或安卓版。

## 许可与数据

项目署名为 yxx869277-prog，采用作者选定的 [MIT License](LICENSE)。第三方组件保持各自许可，见 [THIRD_PARTY.md](THIRD_PARTY.md)。默认采集与存储规则见 [PRIVACY.md](PRIVACY.md)。
